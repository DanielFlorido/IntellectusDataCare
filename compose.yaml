
services:
  angular-app:
    build: .
    container_name: angular_dev
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NG_CLI_ANALYTICS=ci
    stdin_open: true
    tty: true
